# Data directories

Data is generated by the project commands and is intentionally not committed.

- `raw/` contains unchanged source downloads and provenance metadata.
- `processed/` contains normalized, fixed training, validation, and test splits.

See the root README for the required command order. Never use a test split during model development.
